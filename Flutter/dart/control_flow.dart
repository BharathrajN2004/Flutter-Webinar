void main() {
// for loops
// while and do while loops
// break and continue
// You can also manipulate control flow in Dart using:

// Branching, like if and switch
// Exceptions, like try, catch, and throw
  // var message = StringBuffer('Dart is fun');
  // for (var i = 0; i < 5; i++) {
  //   message.write('!');
  // }
  // print(message);
  // List collection = [1, 2, 3];
  // for (var i in collection) {
  //   print(i);
  // }
  // collection.forEach((v) {
  //   print(v);
  // });
  // collection.forEach(print);

//   int i = 1;
//   while (i <= 5) {
//     print(i);
//     ++i;
//   }

//   int i = 10;
// print("Dart do-while loop example");

// do{

//         print(i);
//         i++;

// }while(i<=20);

// Iterable
// A collection is an object that represents a group of objects,
// which are called elements. Iterables are a kind of collection.

  // Iterable<int> iterable = [1, 2, 3];
  // final numbers = <int>[1, 2, 3, 5, 6, 7,];
  // var result = numbers.firstWhere((element) => element < 5); // 1
  // result = numbers.lastWhere((element) => element > 5);
}


// HandOver to barath